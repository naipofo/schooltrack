targets:
  $default:
    builders:
      ferry_generator|graphql_builder:
        enabled: true
        options:
          schema: school_track|lib/gql/requests/schema.graphql
      ferry_generator|serializer_builder:
        enabled: true
        options:
          schema: school_track|lib/gql/requests/schema.graphql
